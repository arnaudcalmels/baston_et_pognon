import React from 'react';
// import PropTypes from 'prop-types';

const Home = (openModal) => {
    return (
        <>
            <p>Je suis dans Home</p>
        </>
    );
};

// Home.propTypes = {
    
// };

export default Home;