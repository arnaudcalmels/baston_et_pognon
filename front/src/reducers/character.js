import {
  
} from '../actions/types';

const initialState = {
  id: '',
};

const reducer = (oldState = initialState, action) => {
  switch (action.type) {
    default: 
      return oldState;
  }
};

export default reducer;