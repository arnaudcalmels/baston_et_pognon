import React from 'react';

import styles from './game.module.scss';

const Game = (props) => {
  return (
    <div className={styles['main']}>
      <p>Prochainement...</p>
    </div>
  );
};

export default Game;
