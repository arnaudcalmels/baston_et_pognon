import React from 'react';

// import PropTypes from 'prop-types';

const Game = (props) => {
    return (
        <>
            <p>Je suis dans Game</p>
        </>
    );
};

// Game.propTypes = {
    
// };

export default Game;